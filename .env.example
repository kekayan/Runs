# GitHub OAuth Configuration
# Register your OAuth app at: https://github.com/settings/developers
# 
# These are app identifiers, not secrets. They identify your OAuth app to GitHub.
# The actual secret is your Client Secret, which should be kept private.

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional: Override the redirect URI (must match your GitHub OAuth app settings)
# GITHUB_REDIRECT_URI=dev.kekayan.runs://oauth-callback
